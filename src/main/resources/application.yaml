spring:
  application.name: "code-challenge"

  rabbitmq:
    host: "localhost"
    port: "5672"
    username: "guest"
    password: "guest"

  data:
    mongodb:
      uri: "mongodb://localhost:27017/mongodatabase"
